services:
  - type: web
    name: southybot
    runtime: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python server.py"
    envVars:
      - key: SOUTHYBOT_KB_SOURCE
        value: json
